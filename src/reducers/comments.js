const comments = (state = {}, action, entireState) => {
    switch(action.type) {
    case 'ADD_COMMENT':
        return state

    case 'EDIT_COMMENT':
        return state

    case 'UPDATE_COMMENT':
        return state

    case 'DELETE_COMMENT':
        return state

    default:
        return state;
    }
};

export default comments;