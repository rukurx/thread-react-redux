const thread = (state, action) => {
    switch(action.type) {

    case 'EDIT_THREAD':
        return state

    case 'UPDATE_THREAD':
        return state

    case 'DELETE_THREAD':
        return state;

    default:
        return state;
    }
};

export default thread;